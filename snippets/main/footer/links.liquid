<div class="row center footer-links footer-line">
    {% if settings.footer_company_information_enable %}    
        <div class="col-md-2">
            <h4>{{ 'layout.footer.company_information' | t }}</h4>
            <ul>
                {% for link in linklists[settings.footer_company_information_linklist].links %}
                    <li><a href="{{ link.url }}">{{ link.title }}</a></li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}
    {% if settings.footer_customer_support_enable %}
        <div class="col-md-2">
            <h4>{{ 'layout.footer.customer_support' | t }}</h4>
            <ul>
                {% for link in linklists[settings.footer_customer_support_linklist].links %}
                    <li><a href="{{ link.url }}">{{ link.title }}</a></li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}
    {% if settings.footer_order_support_enable %}
        <div class="col-md-2">
            <h4>{{ 'layout.footer.order_support' | t }}</h4>
            <ul>
                {% for link in linklists[settings.footer_order_support_linklist].links %}
                    <li><a href="{{ link.url }}">{{ link.title }}</a></li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}
    {% if settings.footer_resources_enable %}
        <div class="col-md-2">
            <h4>{{ 'layout.footer.resources' | t }}</h4>
            <ul>
                {% for link in linklists[settings.footer_resources_linklist].links %}
                    <li><a href="{{ link.url }}">{{ link.title }}</a></li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}
    {% if settings.footer_services_enable %}
        <div class="col-md-2">
            <h4>{{ 'layout.footer.services' | t }}</h4>
            <ul>
                {% for link in linklists[settings.footer_services_linklist].links %}
                    <li><a href="{{ link.url }}">{{ link.title }}</a></li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}
</div>
